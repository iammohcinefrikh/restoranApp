<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Restoran - Ajouter un repas</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

      * {
        font-family: "Roboto", sans-serif;
        box-sizing: border-box;
      }

      body {
        margin: 0;
        width: 100vw;
        height: 100vh;
        display: flex;
        align-items: center;
      }

      .main-container {
        display: flex;
        flex-direction: column;
        width: 420px;
        margin: 0 auto;
      }

      .main-container form {
        display: flex;
        flex-direction: column;
      }

      .main-container form label {
        margin-top: 24px;
        margin-bottom: 6px;
      }

      .main-container form input {
        height: 32px;
        font-size: 14px;
      }

      .main-container form textarea {
        max-width: 420px;
        min-width: 420px;
        max-height: 180px;
        min-height: 82px;
      }

      .main-container form button {
        height: 32px;
        margin-top: 24px;
      }

      .main-container form select {
        height: 32px;
      }

      .hidden {
        display: none;
      }
    </style>
  </head>
  <body>
    <main class="main-container">
      <h1>Ajouter un repas</h1>
      <form id="add-plate-form" action="/post-plate" method="POST" enctype="multipart/form-data">
        <label for="name">Nom</label>
        <input type="text" name="plateName" id="name-input" required>
        <label for="price">Prix</label>
        <input type="number" name="platePrice" id="price-input" required>
        <label for="description">Description</label>
        <textarea name="plateDescription" id="description" required></textarea>
        <label for="image">Image</label>
        <input type="file" name="plateImage" accept="image/*" required>
        <label for="category">Catégorie</label>
        <select name="categoryId">
          <option value="">Sélectionner une catégorie...</option>
          <% for(let i = 0; i < category.length ; i++) { %>
            <option value="<%= i + 1 %>"><%= category[i] %></option>
          <% } %>
        </select>
        <button type="submit" value="Submit">Soumettre</button>
      </form>
    </main>
  </body>
</html>
