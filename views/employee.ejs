<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Restoran - Ajouter un employé</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

      * {
        font-family: "Roboto", sans-serif;
        box-sizing: border-box;
      }

      body {
        margin: 0;
        width: 100vw;
        height: 100vh;
        display: flex;
        align-items: center;
      }

      .main-container {
        display: flex;
        flex-direction: column;
        width: 420px;
        margin: 0 auto;
      }

      .main-container form {
        display: flex;
        flex-direction: column;
      }

      .main-container form label {
        margin-top: 24px;
        margin-bottom: 6px;
      }

      .main-container form input {
        height: 32px;
        font-size: 14px;
      }

      .main-container form textarea {
        max-width: 420px;
        min-width: 420px;
        max-height: 180px;
        min-height: 82px;
      }

      .main-container form button {
        height: 32px;
        margin-top: 24px;
      }

      .main-container form select {
        height: 32px;
      }

      .hidden {
        display: none;
      }
    </style>
  </head>
  <body>
    <main class="main-container">
      <h1>Ajouter un employé</h1>
      <form id="add-emplyee-form" action="/post-employee" method="POST" enctype="multipart/form-data">
        <label for="employeeName">Nom</label>
        <input type="text" name="employeeName" id="name-input" required>
        <label for="employeeFunction">Désignation</label>
        <input type="text" name="employeeFunction" id="price-input" required>
        <label for="employeeImage">Image</label>
        <input type="file" name="employeeImage" accept="image/*" required>
        <label for="restaurantId">Restaurant</label>
        <select name="restaurantId">
          <option value="">Sélectionner une restaurant...</option>
          <% for(let i = 0; i < restaurant.length ; i++) { %>
            <option value="<%= i + 1 %>"><%= restaurant[i] %></option>
          <% } %>
        </select>
        <button type="submit" value="Submit">Soumettre</button>
      </form>
    </main>
  </body>
</html>